# Copy to <repo root>/.env.fullpilot.local (gitignored). Loaded by
# `npm run dev:run` on top of upstream's local dev env from the Makefile.

# Gmail mailbox connections. Thunderbird's public desktop client + the
# localhost-redirect paste flow, same as production.
BOX_GOOGLE_CLIENT_ID=406964657835-aq8lmia8j95dhl1a2bvharmfk3t1hgqj.apps.googleusercontent.com
BOX_GOOGLE_CLIENT_SECRET=kSmqreRr0qwBWJgbf5Y-PjSU
BOX_GOOGLE_REDIRECT_MODE=localhost

# Microsoft 365, once an Entra app registration exists.
#BOX_OUTLOOK_CLIENT_ID=
#BOX_OUTLOOK_CLIENT_SECRET=
